{% extends "admin/base.html.twig" %}

{% block main %}

	<h1>{{ product.name }}</h1>
	<h2>{{ product.price }}</h2>
	<h2>{{ product.stock }}</h2>

	<h3>{{ product.category.name }}</h3>
	<h3>{{ product.licence.name }}</h3>


{% endblock %}
